{
  "buildCommand": "npx vite build && mkdir -p public && cp -r dist/public/* public/",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index"
    }
  ]
}
