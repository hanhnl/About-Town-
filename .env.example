# ========================================
# About Town - Environment Configuration
# ========================================

# Database Configuration (optional)
DATABASE_URL=postgresql://user:password@host:5432/database

# Optional: Set to "true" to run database seeding on first deploy
SEED_DATABASE=false

# ========================================
# OpenStates API Configuration (RECOMMENDED)
# ========================================
# PRIMARY source for live Maryland state legislation data
# FREE TIER: 1,000 requests/day (sufficient for most apps)
# SIGN UP: https://openstates.org/api/register/ (instant approval)
#
# For local development: Copy this file to .env and add your key
# For Vercel: Add OPENSTATES_API_KEY in Project Settings > Environment Variables
#
# VERIFY IT'S WORKING:
# Visit /api/debug/status to check the API connection
# You should see: "✅ OpenStates API is working!"
#
OPENSTATES_API_KEY=

# ========================================
# LegiScan API Configuration (FALLBACK - Optional)
# ========================================
# FALLBACK source if OpenStates fails (most users don't need this)
# SIGN UP: https://legiscan.com/legiscan (approval takes 24+ hours)
#
LEGISCAN_API_KEY=

# Node Environment (automatically set by Vercel)
NODE_ENV=production
