# Database Configuration
DATABASE_URL=postgresql://user:password@host:5432/database

# Optional: Set to "true" to run database seeding on first deploy
SEED_DATABASE=false

# LegiScan API (Optional - for state legislature data)
# Get your API key from https://legiscan.com/legiscan
LEGISCAN_API_KEY=

# Node Environment (automatically set by Vercel)
NODE_ENV=production
